# SFX Library · erfana

> All assets generated via the ElevenLabs Sound Generation API – Apple-keynote-grade audio quality.
> A production-grade SFX asset library covering erfana animations / presentations / product demos.

**Asset location**: `assets/sfx/<category>/<name>.mp3`
**Total**: 37 SFX (30 batch-generated + 7 v7b retained)
**Generation model**: ElevenLabs Sound Generation API (prompt_influence 0.4)
**Audio quality**: 44.1kHz MP3, Apple-keynote-grade clarity, no added reverb

---

## Directory structure

```
assets/sfx/
├── keyboard/      type, type-fast, delete-key, space-tap, enter
├── ui/            click, click-soft, focus, hover-subtle, tap-finger, toggle-on
├── transition/    whoosh, whoosh-fast, swipe-horizontal, slide-in, dissolve
├── container/     card-snap, card-flip, stack-collapse, modal-open
├── feedback/      success-chime, error-tone, notification-pop, achievement
├── progress/      loading-tick, complete-done, generate-start
├── impact/        logo-reveal, logo-reveal-v2, brand-stamp, drop-thud
├── magic/         sparkle, ai-process, transform
└── terminal/      command-execute, output-appear, cursor-blink
```

---

## Quick index

### ⌨️ Keyboard (keyboard input)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/keyboard/type.mp3` | 0.5s | single keystroke (mechanical keyboard single key) | mechanical keyboard single key press |
| `sfx/keyboard/type-fast.mp3` | 1.5s | continuous fast typing (demoing prompt input) | fast continuous typing rhythm, apple magic keyboard |
| `sfx/keyboard/delete-key.mp3` | 0.5s | backspace delete | single backspace key, low pitched thud |
| `sfx/keyboard/space-tap.mp3` | 0.5s | soft spacebar tap | soft spacebar tap, wide flat |
| `sfx/keyboard/enter.mp3` | 0.5s | return / confirm (v7b retained) | enter key press, crisp tactile |

### 🎯 UI (interface interactions)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/ui/click.mp3` | 0.5s | standard UI click (v7b retained) | crisp modern interface click |
| `sfx/ui/click-soft.mp3` | 0.5s | soft UI click (secondary buttons / links) | soft gentle button click, mid pitched |
| `sfx/ui/focus.mp3` | 0.5s | element focus / selection (v7b retained) | subtle focus tone, element highlight |
| `sfx/ui/hover-subtle.mp3` | 0.5s | hover hint (microsecond-level feedback) | barely audible tick, air whisper |
| `sfx/ui/tap-finger.mp3` | 0.5s | mobile tap (iOS interface) | finger tap on touchscreen, muted thud |
| `sfx/ui/toggle-on.mp3` | 0.5s | toggle switch on | ios toggle switch flip, satisfying click |

### 🌊 Transition (transitions)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/transition/whoosh.mp3` | 0.5s | standard whoosh (v7b retained) | air whoosh transition |
| `sfx/transition/whoosh-fast.mp3` | 0.6s | fast whoosh (title flash-in, tab switching) | quick fast air whoosh, cinematic |
| `sfx/transition/swipe-horizontal.mp3` | 0.7s | horizontal swipe (carousel, tab switch) | smooth left-to-right air movement |
| `sfx/transition/slide-in.mp3` | 0.6s | element slide-in (side panel, drawer) | smooth soft whoosh with arrival |
| `sfx/transition/dissolve.mp3` | 0.8s | soft dissolve (image fade-in/out) | soft dissolve, airy shimmer |

### 🃏 Container (cards / containers)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/container/card-snap.mp3` | 0.5s | card snap into place (v7b retained) | card snap into place |
| `sfx/container/card-flip.mp3` | 0.7s | card flip (front/back swap) | playing card flip, crisp snap |
| `sfx/container/stack-collapse.mp3` | 0.8s | stack collapse (list aggregation) | cards stacking, paper taps collapsing |
| `sfx/container/modal-open.mp3` | 0.6s | modal open | modal popping open, whoosh + thud |

### 🔔 Feedback (notifications / feedback)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/feedback/success-chime.mp3` | 1.0s | success cue (payment success, task completed) | two ascending bell tones, ios-style |
| `sfx/feedback/error-tone.mp3` | 0.7s | error cue (warning, failure) | descending two-note warning, soft |
| `sfx/feedback/notification-pop.mp3` | 0.6s | message pop (toast, notification) | notification bloop, ios message alert |
| `sfx/feedback/achievement.mp3` | 1.5s | achievement unlocked (milestone, badge) | triumphant rising arpeggio, game-style |

### ⏳ Progress (progress / state)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/progress/loading-tick.mp3` | 0.5s | loading tick (progress-bar beat) | soft short pulse, minimal ambient |
| `sfx/progress/complete-done.mp3` | 0.8s | step complete | two ascending satisfying tones |
| `sfx/progress/generate-start.mp3` | 0.8s | AI generation starts | soft rising shimmer, magical whoosh |

### 💥 Impact (brand / impact)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/impact/logo-reveal.mp3` | 0.7s | logo impact (v7b retained) | logo reveal thud |
| `sfx/impact/logo-reveal-v2.mp3` | 1.5s | longer logo impact (cinematic) | cinematic bass hit with shimmer tail |
| `sfx/impact/brand-stamp.mp3` | 1.0s | stamp impact (certification, sealing) | rubber stamp thud, paper contact |
| `sfx/impact/drop-thud.mp3` | 0.7s | object landing (insert, place) | heavy thud, wood surface contact |

### ✨ Magic (AI transforms)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/magic/sparkle.mp3` | 0.8s | magical sparkle (AI highlight, surprise) | bright twinkling stars, fairy dust |
| `sfx/magic/ai-process.mp3` | 1.2s | AI processing tone (thinking state) | modulating digital hum with shimmer |
| `sfx/magic/transform.mp3` | 1.0s | transform transition (morph effect) | rising shimmer whoosh with sparkle tail |

### 💻 Terminal (command line)

| File | Duration | Use | Prompt notes |
|---|---|---|---|
| `sfx/terminal/command-execute.mp3` | 0.5s | command execution | crisp digital beep with tick, hacker ui |
| `sfx/terminal/output-appear.mp3` | 0.6s | output appears | rapid digital ticks, retro printout |
| `sfx/terminal/cursor-blink.mp3` | 0.5s | cursor blink | subtle soft digital pulse, rhythmic |

---

## Recommended pairings by scene

### 💻 Terminal interaction demo
```
type (0.5s) -> enter (0.5s) -> command-execute (0.5s) -> output-appear (0.6s)
```
Loop element: `cursor-blink` as the idle ambient sound.

### 🃏 Card-selection flow
```
hover-subtle (0.5s, UI hover) -> click-soft (0.5s, click) -> card-snap (0.5s, settle)
```
Or upgraded: `card-flip` for front/back swap.

### 🤖 Full AI-generation flow
```
generate-start (0.8s, kick-off) -> ai-process (1.2s, processing) -> sparkle (0.8s, flash) -> complete-done (0.8s, done)
```
On error, swap `complete-done` for `error-tone`.

### 🎬 Logo reveal (brand moment)
```
whoosh-fast (0.6s, setup) -> logo-reveal-v2 (1.5s, landing) -> sparkle (0.8s, tail)
```
Simple version: `whoosh -> logo-reveal` (the v7b two-piece set).

### 📱 UI interaction demo (mobile)
```
tap-finger (0.5s, click) -> slide-in (0.6s, panel slide-in) -> toggle-on (0.5s, switch)
```
On completion: `success-chime` or `notification-pop`.

### 📊 Data viz / dashboard
```
loading-tick (0.5s, beat) x N -> complete-done (0.8s, data lands) -> achievement (1.5s, dramatic finish)
```

### 🎯 Form-submit flow
```
click-soft (0.5s) -> loading-tick x2 (1.0s) -> success-chime (1.0s)
```
Failure branch: `error-tone (0.7s)`.

### 🪄 Magic-transform scene
```
whoosh-fast (0.6s) -> transform (1.0s) -> sparkle (0.8s)
```
Suitable for: element morph, before/after comparisons, "AI rewrite" demos.

---

## Usage rules

### Volume guidance (from the apple-gallery-showcase.md two-track audio system)
- **SFX main track**: `1.0` (no attenuation)
- **BGM background track**: `0.4 ~ 0.5` (SFX clearly cuts through)
- **Multi-SFX overlap**: use `amix=inputs=N:duration=longest:normalize=0` to preserve dynamic range

### ffmpeg mux templates
```bash
# Single SFX aligned to a timestamp:
ffmpeg -i video.mp4 -itsoffset 2.5 -i sfx/ui/click.mp3 \
  -filter_complex "[0:a][1:a]amix=inputs=2:duration=longest:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4

# Multiple SFX + BGM:
ffmpeg -i video.mp4 \
  -itsoffset 1.0 -i sfx/transition/whoosh-fast.mp3 \
  -itsoffset 1.6 -i sfx/impact/logo-reveal-v2.mp3 \
  -i bgm.mp3 \
  -filter_complex "[3:a]volume=0.4[bgm];[0:a][1:a][2:a][bgm]amix=inputs=4:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4
```

### Selection decision tree
1. **Tactile action** (typing / clicking / swiping) -> `keyboard/` or `ui/`
2. **Element entrance / exit** -> `transition/`
3. **Container-level operation** (card / modal) -> `container/`
4. **Status feedback** (success / failure / notification) -> `feedback/`
5. **Progress / passage of time** -> `progress/`
6. **Brand landing / important moment** -> `impact/`
7. **AI magic / transform** -> `magic/`
8. **Command line / code demo** -> `terminal/`

### Avoid sound pile-ups
- At any single moment, `max 2 SFX` concurrent
- When BGM is below 0.3, you can stack 3
- During a brand impact moment, clear other SFX (leave 0.2s of silence before the landing)

---

## Prompt-writing principles (for reuse)

Reference style: `apple keynote, tight, minimal, no reverb unless ambient, crisp, elegant`

**Three components of a good prompt**:
1. **Sound physics description**: what object, what action ("mechanical keyboard single key press")
2. **Material / style qualifier**: apple-style / ios-style / cinematic / retro
3. **Negative exclusions**: no reverb / clean studio / minimal

❌ "click sound"
✅ "crisp ui button click, clean modern interface sound, apple-style, high pitched"

❌ "magic"
✅ "bright twinkling stars sound, high pitched glittery chime, fairy dust"

---

## See also
- Two-track audio system and ffmpeg muxing: `apple-gallery-showcase.md`
- Original generation script: `/tmp/gen_sfx_batch.sh` (one-shot batch generator)
