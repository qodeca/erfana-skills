# ACME Corp illustration style (example)

Placeholder. Soft geometric shapes, generous whitespace, pastel palette tuned to ACME Slate / Teal. Replace this entire file when you fork `example-acme/` for a real brand.

The point of this file existing is to demonstrate that `imagery.illustrationStyle` and `voice.guide` are separate concerns that point at separate documents – fixing the SRP smell flagged in v2.3.0 review.
